// Node Commands

// node --version -> To check the version
// npm --version  -. check the npm version

// Bach command shortcut -> CTRL+ALT+t

//node --version && npm --version

// Install xcode -> shell xcode-select --install

// We can also install brew
   // -> brew install node